# Agent Engineer 架构

<p align="center">
  <strong>用简单配置构建专业级 AI 代理</strong>
</p>

<p align="center">
  基于 LangGraph 的智能代理创建、部署和管理系统
</p>

<p align="center">
  <a href="#特性">特性</a> •
  <a href="#架构">架构</a> •
  <a href="#快速开始">快速开始</a> •
  <a href="#范式">范式</a> •
  <a href="#api">API</a>
</p>

---

## ✨ 特性

### 多范式支持

| 范式 | 说明 | 适用场景 |
|------|------|----------|
| **React** | 标准思考-行动-观察循环 | 顺序推理、单一任务 |
| **Surprise** | 从意外结果中自我改进 | 自适应学习、错误恢复 |
| **Peer** | 多代理协作辩论 | 多视角分析、共识构建 |
| **Hierarchical** | 管理者-工作者结构 | 复杂工作流、任务分发 |
| **Evolutionary** | 基于种群的优化 | 策略优化、参数调优 |

### 动态代理生成
- 📝 自然语言描述代理
- 🔧 自动生成 LangGraph 代码
- 👁️ 可视化图形预览
- 🚀 一键部署

### 专业架构
- 清晰、可维护的代码结构
- 完整的文档
- 商业级设计模式
- 可扩展插件系统

---

## 🏗️ 架构

```
agent-engineer/
├── frontend/              # Web UI
│   └── index.html        # 单页应用
├── backend/              # API 服务
│   ├── server.py         # Flask API 端点
│   ├── generator.py      # LangGraph 代码生成
│   └── requirements.txt  # Python 依赖
├── graph_lib/            # 核心代理模式
│   ├── agents.py         # 基础代理类
│   └── paradigms.py      # 范式实现
├── examples/             # 示例配置
│   ├── research_agent.json
│   ├── debate_team.json
│   └── adaptive_solver.json
└── README.md
```

---

## 🚀 快速开始

### 前置要求
- Python 3.10+
- Node.js 18+
- OpenAI API Key（或兼容 LLM）

### 安装

```bash
# 克隆仓库
git clone https://github.com/lohasle/agent-engineer.git
cd agent-engineer

# 安装后端依赖
pip install -r backend/requirements.txt

# 启动后端
cd backend
python server.py

# 打开前端
# 在浏览器中打开 frontend/index.html
```

### 基本使用

```python
from backend.generator import generate_agent

# 定义代理配置
config = {
    "name": "我的研究代理",
    "description": "一个研究主题的智能代理",
    "paradigm": "react",
    "tools": ["search", "browser", "file_write"],
    "model": "gpt-4",
    "max_iterations": 10
}

# 生成 LangGraph 代码
files = generate_agent(config)

# 保存生成的代码
for filename, content in files.items():
    with open(filename, 'w') as f:
        f.write(content)
```

---

## 🔧 范式详解

### React 范式
标准思考-行动-观察循环，适合顺序推理。

**图结构：**
```
思考 → 行动 → 观察 → (重复或结束)
```

**适用场景：**
- 单代理任务完成
- 顺序信息处理
- 简单自动化工作流

### Surprise 范式
从意外结果中自我改进的代理。

**核心特性：**
- 惊喜检测和分析
- 自动策略适应
- 从错误中学习

**适用场景：**
- 自适应学习系统
- 错误恢复和弹性
- 策略优化

### Peer 范式
通过辩论和综合进行多代理协作。

**核心特性：**
- 并行对等处理
- 观点综合
- 辩论编排

**适用场景：**
- 多视角分析
- 共识构建
- 协作问题解决

### Hierarchical 范式
任务分发的管理者-工作者结构。

**核心特性：**
- 任务分解
- 工作者选择和委托
- 进度监控和汇总

**适用场景：**
- 复杂工作流编排
- 任务委托
- 可扩展处理

### Evolutionary 范式
持续改进的基于种群进化。

**核心特性：**
- 锦标赛选择
- 交叉和变异
- 适应度评估

**适用场景：**
- 策略优化
- 参数调优
- 自适应系统

---

## 📚 API 参考

### REST 端点

#### GET /api/paradigms
列出所有可用范式。

```json
{
  "paradigms": [
    {
      "id": "react",
      "name": "React",
      "description": "标准思考-行动-观察循环",
      "complexity": "low"
    }
  ]
}
```

#### POST /api/generate
从配置生成 LangGraph 代码。

**请求：**
```json
{
  "name": "我的代理",
  "description": "一个智能代理...",
  "paradigm": "react",
  "tools": ["search", "browser"]
}
```

**响应：**
```json
{
  "status": "success",
  "agent_id": "uuid",
  "files": {
    "agent.py": "...",
    "graph.py": "...",
    "config.json": "...",
    "requirements.txt": "..."
  }
}
```

#### POST /api/execute
使用输入数据运行代理。

---

## 📦 示例

### 研究代理
基于 React 的简单主题研究代理。

```json
{
  "paradigm": "react",
  "tools": ["search", "browser", "file_write"],
  "max_iterations": 10
}
```

### 辩论团队
结构化辩论的多代理系统。

```json
{
  "paradigm": "peer",
  "peers": ["proponent", "opponent", "moderator"]
}
```

### 自适应解决器
带惊喜学习的自我改进代理。

```json
{
  "paradigm": "surprise",
  "learning_config": {
    "learning_rate": 0.1,
    "surprise_threshold": 0.7
  }
}
```

---

## 🧪 测试

```bash
# 运行后端测试
cd backend
python -m pytest tests/

# 运行前端测试
cd frontend
npm test
```

---

## 🔒 安全

- 在后端验证所有输入
- 使用环境变量存储 API 密钥
- 实现 API 端点速率限制
- 执行前清理生成的代码

---

## 📄 许可证

MIT License

---

## 🤝 贡献

1. Fork 仓库
2. 创建特性分支
3. 提交更改
4. 创建 Pull Request

---

## 📞 支持

- GitHub Issues：报告问题和请求功能
- 文档：查看 /docs 获取详细指南

---

<p align="center">
  使用 ❤️ 和 LangGraph 构建
</p>
